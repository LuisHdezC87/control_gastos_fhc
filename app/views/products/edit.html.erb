<h1>Editing Product</h1>

<%= render 'form', product: @product %>

<%= link_to 'Show', project_category_product_path(@project, @category, @product) %> |
<%= link_to 'Back', project_category_products_path(@project, @category, @product) %>
